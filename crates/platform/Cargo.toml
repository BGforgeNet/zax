[package]
name = "zax-platform"
description = "The capability seam: the only route to files, processes, network, archives and the registry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
hex = "0.4.3"
md-5 = "0.11.0"
sha2 = "0.11.0"
thiserror = "2.0.20"
