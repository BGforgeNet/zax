{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "The interface's window: subscribing to an operation's progress. Its own commands need no grant.",
  "windows": ["main"],
  "permissions": ["core:event:allow-listen", "core:event:allow-unlisten"]
}
