name: build

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
          # - os: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build and package
        run: |
          set -xeu -o pipefail
          pip install -r requirements.txt
          ls C:/hostedtoolcache/windows/Python/3.7.9/x64/lib/site-packages/PySide2/plugins/platforms
          python setup.py py2exe
        shell: bash

      - name: Upload artifacts
        uses: actions/upload-artifact@v2
        if: "!startsWith(github.ref, 'refs/tags/') && (matrix.os == 'windows-latest')"
        with:
          name: dist
          path: dist/*
